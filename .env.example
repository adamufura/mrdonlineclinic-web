# Browser calls same origin (Vite dev server); /api and /socket.io are proxied to the backend.
VITE_API_BASE_URL=http://127.0.0.1:5173/api/v1
VITE_SOCKET_URL=http://127.0.0.1:5173
# Used for returnUrl / email links in dev (backend CLIENT_URL should match)
VITE_APP_URL=http://127.0.0.1:5173
